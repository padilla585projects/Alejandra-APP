================================================================================
  ALEJANDRA APP — REFERENCIA COMPLETA DEL PROYECTO
  Leer este archivo AL INICIO de cada sesión para continuar sin problemas.
  Última actualización: 23/04/2026
  ⚠️  NORMA: actualizar ESTE archivo + ESTADO_APP.txt después de CADA cambio.
================================================================================

================================================================================
  PROTOCOLO DE SESIÓN — LEER ESTO PRIMERO
================================================================================

AL INICIO DE CADA SESIÓN (cualquier ordenador, cualquier chat):
  1. Leer REFERENCIA_PROYECTO.txt (raíz del proyecto) — versiones y normas
  2. Leer NuevaApp/ESTADO_APP.txt — últimos cambios detallados
  3. Leer Bobinaap/ESTADO_APP.txt — estado app vieja
  4. ⚠️  Leer NuevaApp/IDEAS_PENDIENTES.txt — bugs y mejoras pendientes
  → Con esos 4 archivos ya tienes el contexto completo para continuar

  IDEAS_PENDIENTES.txt: lista SIEMPRE actualizada de bugs, mejoras y features
  pendientes. Antes de codificar cualquier cosa, revisar si afecta a algún ítem
  de esa lista. Al resolver un ítem, marcarlo como [✓ HECHO vX.XX] en ese archivo.

AL TERMINAR CUALQUIER CAMBIO DE CÓDIGO (obligatorio, sin excepción):
  Actualizar estos archivos y hacer push:
  1. NuevaApp/ESTADO_APP.txt        → añadir entrada vX.XX + actualizar cabecera
  2. NuevaApp/REFERENCIA_PROYECTO.txt → actualizar versiones + historial reciente
  3. NuevaApp/IDEAS_PENDIENTES.txt  → marcar [✓ HECHO vX.XX] en los ítems resueltos
  4. Bobinaap/ESTADO_APP.txt        → actualizar línea VERSIÓN ACTUAL
  5. Bobinaap/REFERENCIA_PROYECTO.txt → actualizar versiones + historial reciente
  6. REFERENCIA_PROYECTO.txt (raíz) → actualizar versiones + historial reciente
     ⚠️ Este archivo NO está en git — solo actualizar en local

  Push app nueva:  git -c credential.helper= push https://padilla585projects:
                   <TOKEN>@github.com/padilla585projects/Alejandra-APP.git main
  Push app vieja:  git -c credential.helper= push https://padilla585:
                   <TOKEN>@github.com/padilla585/Bobinaap.git main
  (tokens en Bobinaap/NUEVA_CUENTA.txt)

SI CAMBIAS DE ORDENADOR:
  Antes de empezar a trabajar, sincronizar local con GitHub:
  git -C <ruta_NuevaApp> fetch origin && git -C <ruta_NuevaApp> reset --hard origin/main
  git -C <ruta_Bobinaap> fetch origin && git -C <ruta_Bobinaap> reset --hard origin/main

⚠️  NORMA OBLIGATORIA — CADA VEZ QUE SE TOCA EL CÓDIGO:
  1. Subir la versión de la app (vX.XX → vX.XX+1)
     · version.json       → {"v":"X.XX"}
     · sw.js              → const CACHE = 'alejandra-vX.XX'
     · index.html         → const APP_VERSION = 'X.XX'
     Los tres a la vez, siempre. Sin excepción.
  2. Actualizar los 5 archivos .txt con los cambios realizados (ver sección anterior)
  3. Deploy y push — DOS pasos SIEMPRE obligatorios si se toca worker.js:
     ⚠️  git push solo publica el FRONTEND (GitHub Pages).
     ⚠️  El WORKER es independiente — hay que desplegarlo por separado con curl.
     ⚠️  Si no se hace el deploy del worker, los endpoints nuevos NO existen en producción.

     CHECKLIST DE CIERRE DE SESIÓN:
     [ ] ¿Cambié worker.js?  → deploy curl (ver sección COMANDOS DE DEPLOY)
     [ ] ¿Cambié index.html/sw.js/version.json?  → git push
     [ ] ¿Cambié ambos?  → deploy curl PRIMERO, luego git push
     Nunca cerrar una sesión sin haber hecho los dos pasos que correspondan.

REGLA DE DEPLOY:
  - BUGS      → ambas apps siempre (worker + frontend + push ambos repos)
  - MEJORAS   → solo app nueva
  - En ambos casos: subir versión + actualizar txts + deploy worker + git push

ÚLTIMO CAMBIO REALIZADO:
  v4.92 [nueva] — 23/04/2026
  · NEW-12: NFC en Escanear (leer + escribir tags, Chrome Android 89+)

  v4.91 [nueva] — 23/04/2026
  · NEW-08: chat interno entre empleados (panel 💬, polling 15s, D1 chat_mensajes)

  v4.90 [nueva] — 23/04/2026
  · NEW-02: wizard de bienvenida primera vez (4 slides + dots)

  v4.89 [nueva] — 23/04/2026
  · BUG-009: polling reducido + pausa Page Visibility API (ahorro batería)

  v4.88 [nueva] — 23/04/2026
  · MEJ-137: renombrar carpetas Docs completo (botón ✏️, raíz + subcarpetas)

  v4.87 [nueva] — 23/04/2026
  · BUG: selector visual obra solo para superadmin/empresa_admin (no encargados)

  v4.86 [nueva] — 23/04/2026
  · BUG-048: icono SVG en botón Devolución (sustituye ↩ unicode fuera de estilo)
  · MEJ-044: modal descripción completa al pulsar notas en cards de inventario
  · MEJ-058: buscarMaquina busca también bobinas; buscarItemSeg filtra empresa_id
  · MEJ-136: notas de texto editables en Docs (raíz y dentro de carpetas)
  · MEJ-137: selector "Mover a carpeta" en modal editar archivo
  · MEJ-144: subir archivos/fotos a raíz sin crear carpeta primero
  · MEJ-149: selector visual de obra (cards grandes) tras elegir departamento
  · MEJ-155: subcarpetas dentro de carpetas Docs, con navegación breadcrumb

  v4.83 [nueva] — 22/04/2026
  · Fix superadmin veía inventario de eléctrico al entrar como mecánicas

  v4.71 [nueva] — 22/04/2026
  · BUG-136: botón 👥 "Fichar Equipo" no respondía (funciones MEJ-128 sin exponer al scope global)

  v4.70 [nueva] — 22/04/2026
  · Nueva pestaña 📂 Docs en Eléctrico, Mecánicas y Seguridad
  · Carpetas por obra+departamento: crear, borrar (con todos sus archivos)
  · Archivos dentro de carpetas: subir ≤50MB, descargar con auth, borrar
  · Subir/borrar solo encargado+admin; leer todos los roles
  · Tablas D1: carpetas + docs_dept; R2 key: e{id}/dept/{dept}/{obra}/{carp}/{ts}_{file}

  v4.69 [nueva] — 22/04/2026
  · NEW-03: Documentos de empresa — Ajustes→Empresa sección 📂 con subir/descargar/borrar (R2)
  · MEJ-13: Archivos adjuntos en herramientas — sección 📎 en ficha de herramienta (R2)
  · Infraestructura: bucket R2 "alejandra-app-files" + tabla D1 "archivos" + 4 endpoints worker

  v4.68 [nueva] — 22/04/2026
  · MEJ-131: horarios obra por día (toggle por días, inputs L/M/X/J/V, helper getHorarioParaDia en worker)
  · MEJ-08: departamentos activos por empresa (toggles en Ajustes→Empresa, cache localStorage)
  · BUG-133: user-chip más legible (11px, max-width 130px)
  · #53: filtro 👷 Personal añadido en panel Ideas de Ajustes

  v4.65 [nueva] — 21/04/2026
  · MEJ-132: Telegram bidireccional — botones inline en solicitudes acceso e ideas (worker)
  · BUG-129: navTo() limpia historial; back = "2x para salir"; no duplicados en stack
  · BUG-124: kits herramientas filtran por obra; nueva herramienta pre-rellena obra
  · BUG-120: editarHerrDesdeInv async; fallback API si item no está en cache

  v4.64 [nueva] — 21/04/2026
  · Fix banner actualización: SW_ACTUALIZADO extrae versión del cache name → localStorage antes de recargar

  v4.63 [nueva] — 21/04/2026
  · MEJ-12: fotos en Ideas — botón añadir imagen, compresión auto (≤300KB), preview, thumbnail admin, zoom

  v4.62 [nueva] — 21/04/2026
  · MEJ-11: horarios de obra en Ajustes→Obras (⏰ Horario por obra, upsert)
  · MEJ-11: pre-rellenar horas en modal fichaje según horario de la obra seleccionada
  · QW1: detección automática retrasos — badge "Xmin tarde", estado auto 'retraso'
  · QW2: resumen mensual con horas esperadas (N×8h) y contador retrasos por trabajador
  · QW3: cierre automático de jornada a las 20:00 (cron 0 18 * * *) + Telegram
  · DB: ALTER TABLE fichajes ADD COLUMN minutos_retraso INTEGER DEFAULT 0

  v4.61 [nueva] — 21/04/2026
  · MEJ-09: historial filtrado por departamento (worker + frontend)
  · Fix crítico: getHistorialTabla sin aislamiento empresa — corregido con INNER JOIN
  · Fix: JOIN bobinas en getHistorial era sobre bobina_id → corregido a bobina_codigo
  · SA/EA: badge de dept visible en cada ítem del historial

  v4.60 [nueva] — 21/04/2026
  · Splash screen: muestra versión actual (v4.60) en pequeño bajo el logo
  · Splash screen: firma "Desarrollado por Adrián Padilla" en pie de pantalla
  · Banner de actualización: rediseñado como píldora flotante inferior (z-index 100001)
  · Banner: espera a que desaparezca la splash antes de mostrarse (delay 2.8s primer arranque)

  v4.59 [nueva] — 21/04/2026
  · BUG-56: panel Sesión reestructurado — botones cerrar operarios/encargados/todas siempre visibles
  · BUG-51/55/57: fix 403 injusto en edición/creación/movimiento de inventario Seguridad
  · BUG-59: inventarios filtran por obra para SA/EA (obra_id via query param)
  · BUG-71: filtros Ideas siempre actualizan DOM (hash incluye filtro activo)
  · BUG-60: feedback toast al cambiar estado en Ideas
  · BUG-23: obra-select más ancho (220px), texto ya no se corta
  · Modal aprobación: departamentos corregidos + filtro dinámico por empresa

================================================================================
  RESUMEN EJECUTIVO
================================================================================

Proyecto: App PWA de gestión de obra (bobinas, PEMP, carretillas, herramientas,
          pedidos, personal)
Stack:    Vanilla JS + Cloudflare Workers + D1 (SQLite) + GitHub Pages
Equipo:   Adrian (desarrollador/cliente) + Claude (dev)

HAY DOS APPS EN PARALELO:

  APP VIEJA (producción activa del equipo)
  └─ Una sola empresa fija (CPD Getafe)
  └─ Login solo por código de usuario
  └─ Solo recibe BUGS — no recibe nuevas features
  └─ Versión: v4.19

  APP NUEVA (desarrollo activo, futura sustitución)
  └─ Multi-empresa (cada empresa tiene sus datos aislados por empresa_id)
  └─ Login por código O por email + contraseña
  └─ Wizard de registro de nuevas empresas
  └─ Aquí van todas las mejoras y nuevas features
  └─ Versión: v4.47

REGLA DE DEPLOY:
  - BUGS      → ambas apps siempre
  - MEJORAS   → solo app nueva
  - version.json, sw.js CACHE y badge de versión en index.html → actualizar los tres
    a la vez en cada versión nueva

================================================================================
  APP VIEJA — CREDENCIALES Y ACCESO
================================================================================

GitHub:
  Repo:     https://github.com/padilla585/Bobinaap
  Pages:    https://padilla585.github.io/Bobinaap/
  Usuario:  padilla585
  Token:    → ver NUEVA_CUENTA.txt
  Push:     git -c credential.helper= push https://padilla585:<TOKEN_GITHUB_VIEJO>@github.com/padilla585/Bobinaap.git main

Cloudflare Worker:
  Nombre:   gestibobina-proxy
  URL:      https://gestibobina-proxy.padilla585.workers.dev
  Account:  c100d5a0ea37ee03989fe769c7259b83
  Token:    → ver NUEVA_CUENTA.txt

Cloudflare D1 (base de datos):
  Nombre:   gestibobina-db
  ID:       eac9f2d2-0845-409c-9032-109c563d7eec
  Token D1: → ver NUEVA_CUENTA.txt

Archivos locales:
  Frontend: <RUTA_BASE>\Bobinaap\index.html
  Worker:   <RUTA_BASE>\Bobinaap\worker.js
  SW:       <RUTA_BASE>\Bobinaap\sw.js
  Versión:  <RUTA_BASE>\Bobinaap\version.json

================================================================================
  APP NUEVA — CREDENCIALES Y ACCESO
================================================================================

GitHub:
  Repo:     https://github.com/padilla585projects/Alejandra-APP
  Pages:    https://padilla585projects.github.io/Alejandra-APP/
  Usuario:  padilla585projects
  Token:    → ver NUEVA_CUENTA.txt
  Push:     git push origin main  (credenciales ya guardadas con wrangler OAuth)

Cloudflare Worker:
  Nombre:   alejandra-app-api
  URL:      https://alejandra-app-api.alejandra-app.workers.dev
  Account:  d65ead2b2967bf68ff3848a36cd7b1b4
  Token:    → ver NUEVA_CUENTA.txt

Cloudflare D1 (base de datos):
  Nombre:   alejandra-db
  ID:       0c9eccde-78f1-476d-ac68-bf452bec0c62
  Token D1: → ver NUEVA_CUENTA.txt  (mismo token que Worker)

Archivos locales:
  Frontend: <RUTA_BASE>\NuevaApp\index.html
  Worker:   <RUTA_BASE>\NuevaApp\worker.js
  SW:       <RUTA_BASE>\NuevaApp\sw.js
  Versión:  <RUTA_BASE>\NuevaApp\version.json

================================================================================
  COMANDOS DE DEPLOY — COPIAR Y PEGAR DIRECTAMENTE
================================================================================

── DEPLOY WORKER NUEVO (preferido — Wrangler OAuth) ───────────────────────────
cd "<RUTA_BASE>\NuevaApp"
npx wrangler deploy

── DEPLOY WORKER VIEJO (via curl — sin wrangler) ───────────────────────────────
curl -s -X PUT "https://api.cloudflare.com/client/v4/accounts/c100d5a0ea37ee03989fe769c7259b83/workers/scripts/gestibobina-proxy" \
  -H "Authorization: Bearer <TOKEN_WORKER_VIEJO>" \
  -F "metadata={\"main_module\":\"worker.js\",\"compatibility_date\":\"2024-01-01\",\"bindings\":[{\"type\":\"d1\",\"name\":\"DB\",\"id\":\"eac9f2d2-0845-409c-9032-109c563d7eec\"}]};type=application/json" \
  -F "worker.js=@<RUTA_BASE>\Bobinaap\worker.js;type=application/javascript+module"

⚠️  CRÍTICO: el binding D1 SIEMPRE debe incluirse en el metadata del deploy.
    Sin él, env.DB queda undefined y TODA la app deja de funcionar.
    Con wrangler esto es automático (lee wrangler.toml).

── QUERY D1 NUEVA (wrangler) ───────────────────────────────────────────────────
cd "<RUTA_BASE>\NuevaApp"
npx wrangler d1 execute alejandra-db --remote --command "SELECT ..."

── QUERY D1 NUEVA (curl) ───────────────────────────────────────────────────────
curl -s -X POST "https://api.cloudflare.com/client/v4/accounts/d65ead2b2967bf68ff3848a36cd7b1b4/d1/database/0c9eccde-78f1-476d-ac68-bf452bec0c62/query" \
  -H "Authorization: Bearer <TOKEN_D1_NUEVO>" \
  -H "Content-Type: application/json" \
  -d "{\"sql\":\"SELECT ...\"}"

── GIT PUSH APP NUEVA ──────────────────────────────────────────────────────────
cd "<RUTA_BASE>\NuevaApp"
git add index.html worker.js sw.js version.json ESTADO_APP.txt
git commit -m "descripcion; vX.XX"
git push origin main

── GIT PUSH APP VIEJA ──────────────────────────────────────────────────────────
cd "<RUTA_BASE>\Bobinaap"
git add index.html worker.js sw.js version.json ESTADO_APP.txt
git commit -m "descripcion; vX.XX"
git -c credential.helper= push https://padilla585:<TOKEN_GITHUB_VIEJO>@github.com/padilla585/Bobinaap.git main

================================================================================
  VERSIONES ACTUALES Y CHANGELOGS
================================================================================

APP VIEJA:  v4.19  (sw.js: alejandra-v4.19 | version.json: {"v":"4.19"})
APP NUEVA:  v4.75  (sw.js: alejandra-v4.75 | version.json: {"v":"4.75"})

Historial reciente (app nueva):
  v4.68 [nueva] — MEJ-131 horarios/día + MEJ-08 depts activos por empresa
  v4.67 [nueva] — MEJ-128 fichaje equipo (FAB 👥, modal checkboxes, fichar en bloque)
  v4.66 [nueva] — MEJ-134 foto Telegram, BUG-127 dept pill, BUG-133 user-chip, #53 filtro Personal
  v4.65 [nueva] — BUG-129 botón atrás Android, BUG-124 herramientas/kits por obra, BUG-120 editar herr async, MEJ-132 Telegram bidireccional
  v4.41 [nueva] — Login Google con invitaciones temporales (rol+dept, caduca, 1 uso)
  v4.40 [nueva] — Login con Google OAuth (botón, redirect flow, worker endpoints)
  v4.39 [nueva] — NEW-04 Herramientas Seguridad, NEW-07 exportar PDF+xlsx, NEW-11 café
  v4.38 [nueva] — Fix notificaciones PEMP/carretillas averiadas, fix inventario-seg
  v4.37 [nueva] — NEW-06 splash screen, NEW-09 notificaciones in-app, fix historial
  v4.36 [nueva] — NEW-05 banner "App actualizada"
  v4.35 [nueva] — Fix pantalla selección empresa superadmin
  v4.34 [nueva] — MEJ-05 descripciones departamentos, MEJ-06 marcar/borrar todo Ideas
  v4.33 [nueva] — MEJ-02 escáner herramientas, MEJ-03 ocultar pestañas, MEJ-04 selector empresa
  v4.32 [ambas] — BUG-01 fichaje obra, BUG-03 nombre obra barra superior
  v4.31 [nueva] — MEJ-01 rediseño Pedidos, BUG-02 icono dept
  v4.30 [nueva] — BUG-06 borrar usuario libera credenciales
  v4.26 [nueva] — Google Sheets sync herramientas y kits (Elec/Mec-Herramientas, Kits)
  v4.25 [nueva] — Fix crítico: funciones async no hoistadas a scope global en Chrome
                   (guardarFichaje, guardarHerramienta, etc. → window.X = X)
  v4.24 [nueva] — Fix badge versión dinámico; intervalo update 30s
  v4.23 [nueva] — Fix modales (active→show) en Herramientas, Pedidos, Fichajes
  v4.22 [nueva] — Fix UX botón + herramienta en Escanear/Inventario; FAB fichajes oculto al salir
  v4.21 [nueva] — Rediseño devoluciones: modo ENTRADA/DEVOLUCIÓN en Escanear
  v4.20 [nueva] — Módulo Personal: fichajes Hoy/Semana/Resumen, modal, Excel
                   DB: fichajes, horarios_obra, personal_externo
  v4.19 [nueva] — Módulo Herramientas: kits + inventario, escanear por serie, historial
                   DB: tipos_herramienta, kits_herramientas, herramientas, historial_herramientas
  v4.18 [nueva] — Módulo Pedidos: flujo estados, sync Sheets, Telegram
                   Editar datos empresa. DB: pedidos
  v4.17 [nueva] — Botón ← Atrás + Android; filtro sugerencias por dept
  v4.16 [nueva] — Fix empresa_admin permisos completos; fix panel Mi Empresa
  v4.15 [ambas] — Auto-actualización silenciosa: version.json + SW no cachea HTML
  v4.14 [ambas] — Fix wizard empresa, fix filtros Ideas, fix cerrar sesiones

================================================================================
  ARQUITECTURA TÉCNICA
================================================================================

ROLES:
  superadmin    → código ADMIN_CODE (secret Cloudflare). Ve todo, elige dept.
  empresa_admin → email + contraseña. Admin de su empresa (solo nueva app).
  encargado     → código de usuario. Gestiona su departamento/obra.
  operario      → código de usuario. Solo lectura/scan. Solo ve sus fichajes.

getAuth() devuelve:
  { empresa_id, rol, obra_id, usuario_id, nombre,
    isSuperadmin, isEmpresaAdmin, isAdmin }
  isAdmin = isSuperadmin || isEmpresaAdmin || rol === 'encargado'

DEPARTAMENTOS:
  electrico  → Bobinas + PEMP + Carretillas + Pedidos + Herramientas
  mecanicas  → PEMP + Carretillas + Pedidos + Herramientas
  seguridad  → Solo escáner cross-dept + Inventario de Seguridad
  personal   → Fichajes + Horarios + Asistencia (screenPersonal)

TABLAS D1 (ambas apps):
  bobinas, obras, historial, proveedores, tipos_cable
  usuarios, pemp, carretillas, historial_pemp, historial_carretillas
  logs, sugerencias, inventario_seg, movimientos_seg, tipos_material_seg
  sesiones, config, tipos_pemp, tipos_carretilla, energias_carretilla

TABLAS EXTRA (solo nueva app):
  empresas                          ← multi-tenant
  pedidos                           ← v4.18
  tipos_herramienta                 ← v4.19
  kits_herramientas                 ← v4.19
  herramientas                      ← v4.19
  historial_herramientas            ← v4.19
  fichajes                          ← v4.20  (campo: estado, no tipo)
  horarios_obra                     ← v4.20
  personal_externo                  ← v4.20
  invitaciones                      ← v4.41 (codigo, empresa_id, rol, departamento, expira_at, usado)

MULTI-TENANT (solo nueva app):
  Todas las tablas tienen empresa_id.
  Todas las queries filtran por empresa_id del usuario autenticado.
  Empresa por defecto: id=1 ("Empresa Principal") con los datos reales del equipo.

AUTH:
  Login → POST /verificar → devuelve token de 64 hex chars
  Token se guarda en localStorage como parte de la sesión
  Cada petición lleva header X-Token
  Sesiones almacenadas en tabla D1 `sesiones`

AUTO-ACTUALIZACIÓN (v4.15+):
  Cada app tiene un version.json en la raíz del repo.
  Al arrancar y cada 2 min, la app fetch version.json (cache:no-store + timestamp).
  Si versión != APP_VERSION hardcodeada → limpia caches SW + location.replace().
  El SW no cachea peticiones de navegación (HTML siempre fresco de red).
  → Regla: actualizar version.json, sw.js CACHE y badge de index.html a la vez.

SECRETOS CONFIGURADOS EN WORKERS (app nueva):
  ADMIN_CODE, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID
  GOOGLE_CLIENT_EMAIL, GOOGLE_PRIVATE_KEY
  GOOGLE_SHEET_ID (equipos), GOOGLE_PEDIDOS_SHEET_ID (pedidos)

TELEGRAM:
  Bot: @AlejandraAPP_bot (chat_id: 6965043)

GOOGLE SHEETS (app nueva — sheets propios):
  Equipos:  1SVtsS8nAJWlbFKDt8HxH0MZOJW762U7wRhkyTjIs_Sg
  Pedidos:  1fIrkm6oQRmsVcqHTJstVfLNZJjSumotSQKpuQ6f6lD8
  Pestañas equipos: Elec-Bobinas, Elec-PEMP, Elec-Carretillas,
                    Mec-PEMP, Mec-Carretillas, Seg-Inventario

================================================================================
  ESTADO ACTUAL — QUÉ FUNCIONA (v4.20)
================================================================================

✅ Login multi-dispositivo con tokens D1
✅ Selector de departamento para superadmin/empresa_admin (incl. Personal)
✅ CRUD completo: bobinas, PEMP, carretillas (crear, editar, devolver, transferir, borrar)
✅ Inventario de Seguridad (individual/cantidad, salidas, devoluciones, bajas, editar)
✅ Ficha de máquina a pantalla completa con historial
✅ Escáner QR + OCR (Gemini 2.0 Flash)
✅ Google Sheets sync por pestañas (equipos + pedidos)
✅ Notificaciones Telegram
✅ Catálogos (proveedores, tipos cable/PEMP/carretilla, energías, mat. seg, tipos herramienta)
✅ Sugerencias con estados, filtros y dept
✅ Pedidos: flujo estados, modal crear/editar, filtros, card home
✅ Herramientas: kits + inventario individual, escanear por serie, historial, fechas auto
✅ Personal: fichajes diarios/semanales/mensuales, modal, exportar Excel
✅ Ajustes → Empresa: ver y editar datos (nombre, email, teléfono, dirección, CIF)
✅ Botón ← Atrás en header + botón físico Android
✅ Auto-actualización silenciosa sin interacción del usuario
✅ Exportar Excel (bobinas/PEMP/carretillas + Personal)
✅ PWA instalable (manifest, SW, icons)
✅ Multi-empresa con empresa_id en todas las tablas
✅ Login email + contraseña (PBKDF2), wizard registro empresa

================================================================================
  PENDIENTE / PRÓXIMAS FEATURES
================================================================================

ERRORES CONOCIDOS:
  (ninguno pendiente — todos resueltos en v4.20)

PENDIENTE DE UI (worker ya tiene los endpoints):
  - Gestión personal externo desde frontend
  - Configuración horarios de obra desde Ajustes→Obras
  - Google Sheets sync para fichajes Personal (semanal/mensual)

NUEVAS FUNCIONALIDADES:
  #34 — Planos, fotos, documentos de obra (requiere R2)
  #33 — Departamento Especiales/Teleco
  #9  — App consume batería (pendiente investigar causa)

MULTI-TENANT ROADMAP (app nueva):
  Fase 4 — Almacenamiento R2 (fotos, firma digital, PDFs albarán)
  Fase 5 — Alertas (ITV próxima, máquina averiada X días)
  Fase 6 — Facturación Stripe (basic/pro/enterprise)

================================================================================
  DECISIONES TÉCNICAS IMPORTANTES
================================================================================

1. INSERT...RETURNING no funciona bien en D1.
   Siempre usar .run() + meta.last_row_id para obtener el ID insertado.

2. El binding D1 se pierde en deploy via curl si no se incluye en metadata.
   Con `npx wrangler deploy` es automático (lee wrangler.toml). USAR WRANGLER.

3. apiCall(path, options) — SIEMPRE 2 argumentos.
   MAL: apiCall('/ruta', 'POST', body)
   BIEN: apiCall('/ruta', { method: 'POST', body: JSON.stringify(data) })

4. Credenciales Git: usar `git push origin main` en app nueva (OAuth guardado).
   App vieja: usar -c credential.helper= con token en URL.

5. sendBeacon debe usar Blob({type:'application/json'}) — string plano no funciona.

6. version.json debe actualizarse SIEMPRE junto con sw.js CACHE y badge index.html.

7. fichajes.estado (no 'tipo') — valores: presencia / presente / ausencia /
   retraso / baja / vacaciones / festivo. Default DB: 'presente'.

8. getFichajes devuelve array plano (no {fichajes:[]}). Manejar con:
   Array.isArray(data) ? data : (data.fichajes || [])

9. getTrabajadores devuelve array plano (merge usuarios app + personal_externo).
   Cada item tiene campo 'tipo': 'app' | 'externo'.

10. registrarHistorialHerr / registrarHistorialKitHerr — nombres únicos para evitar
    conflicto con registrarHistorial (bobinas).

11. empresa_admin: tiene acceso total a datos de su empresa.
    getAuth() devuelve isEmpresaAdmin: sesion.rol === 'empresa_admin'.

12. AHORA() helper en worker: new Date().toISOString().slice(0,19).replace('T',' ')
    Usar para todos los timestamps de fechas automáticas.

================================================================================
  ARCHIVOS CLAVE DEL PROYECTO
================================================================================

<RUTA_BASE>\
  ├── REFERENCIA_PROYECTO.txt     ← ESTE ARCHIVO (leer al inicio de sesión)
  ├── Bobinaap\
  │   ├── index.html              ← Frontend app vieja
  │   ├── worker.js               ← Worker Cloudflare app vieja
  │   ├── sw.js                   ← Service Worker (alejandra-v4.17)
  │   ├── version.json            ← {"v":"4.17"}
  │   ├── ESTADO_APP.txt          ← Changelog detallado app vieja
  │   └── NUEVA_CUENTA.txt        ← Tokens y credenciales (NO subir a git)
  └── NuevaApp\
      ├── index.html              ← Frontend app nueva
      ├── worker.js               ← Worker Cloudflare app nueva
      ├── sw.js                   ← Service Worker (alejandra-v4.45)
      ├── version.json            ← {"v":"4.45"}
      ├── wrangler.toml           ← Config deploy (binding D1 incluido)
      ├── ESTADO_APP.txt          ← Changelog detallado app nueva ← LEER
      └── NUEVA_CUENTA.txt        ← Tokens y credenciales (NO subir a git)

================================================================================
  DATOS DE PRODUCCIÓN
================================================================================

Obra activa:       CPD Getafe (código: 001, id: 1, empresa_id: 1)
Empresa principal: "Empresa Principal" (id: 1) — datos reales del equipo
Backup local:      <RUTA_BASE>\backup_db_20260413.sql

================================================================================
