# Backlog arquitectónico — Alejandra 2.0

- Actualizado: 2026-08-07
- Foundation: v0.1 congelada como línea base, sin bloqueos documentales activos (COH-001 y COH-002 cerrados)
- **ARC-021 (2026-08-07): riesgo de proceso aceptado por el Director como práctica habitual, mismo criterio que ARC-014 — ver tabla.**

## Cómo usarlo

Cada ítem conserva estado, evidencia, impacto, alternativas y fase/ADR de destino. Un ítem aprobado se enlaza a especificación, ADR y PR; no se implementa desde este documento.

## Ítems abiertos

| ID | Estado | Tema | Impacto | Evidencia / siguiente decisión |
|---|---|---|---|---|
| ARC-001 | Cerrado | Modelo de riesgo y aprobación humana | Alto | ADR-0003 fija evaluación obligatoria; faltaban umbrales, responsables y catálogo de acciones. **ADR-0006 aceptado el 2026-08-02**: matriz de cuatro niveles (N0–N3) por reversibilidad y alcance. `run_migration` no se retira del catálogo, pero pasa a capacidad administrativa sujeta a autorización explícita, fuera del alcance autónomo del agente. Desbloquea ADR-0004 y con él F-1.1. |
| ARC-002 | Cerrado (diseño) | Gobierno de memoria | Alto | **ADR-0013 aceptado con modificaciones el 2026-08-02**: memoria opt-in (hechos declarados, preferencias, procedimientos, correcciones); inferencias automáticas solo como candidata pendiente de validación humana; caducidad 6 meses (12 para procedimientos aprobados); memoria personal libre, memoria compartida exige aprobación `encargado`+; borrado por derecho de supresión real y sin versión archivada, sin verificación extra desde sesión propia autenticada, reforzada si es sensible/amplia/fuera de sesión; D1 vía el migrador de ADR-0011. Desbloquea que `nucleo-cognitivo/` (F-1.2) construya `memory.js` como interfaz; la persistencia real exige además la migración D1 correspondiente. |
| ARC-003 | Cerrado | Definición de Nexo | Alto | **ADR-0008 aceptado el 2026-08-02**: Nexo se define como interpretación A — capa de integración con sistemas externos (normativa, precios, documentación de fabricantes). No es el Motor de Decisión ni coordinación multiagente. Desbloquea F-2.2 como extensión aditiva de tools existentes. |
| ARC-004 | Cerrado | QA y verificación independiente | Alto | **ADR-0009 aceptado el 2026-08-02**: tres niveles de verificación (determinista, revisión humana asíncrona, explicabilidad), generalizando `CONFIRMO BORRADO`, el botón de Telegram de `alejandra_fixes` y el requisito de traza. La explicabilidad queda como deuda explícita hasta F-4.1, sin bloquear nada mientras tanto. |
| ARC-005 | Mitigado | Promoción deliberada a producción | Crítico | ADR-0001 aceptado e integrado (PR #9, 2026-08-02): CI, despliegues, migraciones y secretos separados; workflows antiguos retirados; `main` protegida; entorno `production` con revisor requerido. Pendiente mover los secretos a nivel de entorno y el ensayo en vacío. La mitigación cubre solo las migraciones lanzadas por workflow, no el DDL que el propio Worker ejecuta en producción: ver ARC-011. |
| ARC-006 | Cerrado | Catálogo de tools y matriz de permisos | Alto | **ADR-0010 aceptado el 2026-08-02**: el nivel de acceso (`acceso`, `cron`, `nivel_riesgo`) pasa a ser metadato declarado en cada tool, validado al registrar, en vez de vivir en los tres `Set` de `lib.js`. Migración incremental, un registro independiente por worker. `nivel_riesgo` usa los niveles N0–N3 de ADR-0006. |
| ARC-007 | Investigación | Fronteras de dominio y extracción incremental | Medio | Monolitos actuales; elegir vertical piloto tras contratos y pruebas. |
| ARC-008 | **Cerrado — implementado, desplegado y verificado** | Observabilidad y métricas cognitivas | Medio | **ADR-0014 aceptado con modificaciones el 2026-08-02**, implementación completa el mismo día: tabla D1 `alejandra_trazas` aplicada y verificada (run `30746110357`); `registrarTraza()` real en los dos Workers, conectado a ARC-013 (`runDDL()`/`ddlPaso()` ya escriben `tipo='ddl_error'`, con minimización/redacción); `/health` con tres estados comprobando D1 y el objeto centinela `_healthcheck/centinela.txt` en R2, versión derivada del `version_metadata` nativo de Cloudflare; `GET /admin/trazas` en `alejandra-app-api`, protegido con `hasRole(s, 'superadmin', 'desarrollador')`. Verificado en vivo contra producción (PR #24, #25). Efecto lateral detectado y corregido el mismo día: `index.html` comparaba el nuevo `version` (UUID) contra `APP_VERSION`, lo que habría forzado recargas falsas — desactivado (PR #26). Healthcheck automático post-despliegue reincorporado (PR #36, 2026-08-02): `deploy-worker.yml`/`deploy-alejandra-agente.yml` consultan `/health` tras desplegar y bloquean el job si es `unhealthy` o no responde; `degraded` queda como advertencia no bloqueante. No queda pendiente. **§8 (trazabilidad de consultas de memoria) resuelto el mismo día:** `consultarMemoria()` real registra `tipo='memoria_consulta'` con los recuerdos devueltos, desbloqueando el paso 3 de `F-2.1-MEMORIA-DECLARAR` (ver `TASKS.md`/`HANDOFF.md`). |
| ARC-009 | Cerrado | Precedencia documental / Libro Maestro | Alto | COH-001 cerrado por ADR-0005: `MASTER_PLAN.md` es la referencia versionada y el original quedó archivado sin autoridad normativa. |
| ARC-010 | Cerrado | Estado del contrato cognitivo | Alto | COH-002 cerrado por ADR-0002: contrato cognitivo aceptado como arquitectura objetivo; implementación bloqueada por dependencias explícitas. |
| ARC-011 | Verificado (fases 1-2); fase 3 con **8 verticales completos** (ciclo de 5 pasos cerrado) y **6 más declarados** (paso 1) | Esquema D1 definido por DDL en tiempo de ejecución | Crítico | **105 de 150 tablas existen solo porque el código las crea** y **27 tablas de producción no las declara nadie** (incluidas `empresas`, `fichajes`, `incidencias`): el esquema no es reproducible desde el repositorio. El contraste con D1 real destapó **3 bugs activos** por `ALTER` silenciados que nunca se aplicaron: `planos.circuitos_json`, `inventario_seg.ubicacion` (el fix de SEG-01 nunca funcionó) y `empresas.retencion_config` (retención RGPD inoperante), **cuyo DDL en runtime también quedó retirado el 2026-08-02** (PR #54). Análisis y acciones propuestas en `docs/architecture/07-INVENTARIO-DDL-RUNTIME.md`. **Fase 3: ADR-0011 aceptado el 2026-08-02** como estrategia (migrador por vertical, empezando por `checklists`, con manifiesto versionado). **Ocho verticales completos (2026-08-02/03), ciclo de 5 pasos cerrado en todos:** `checklists`, `rfis`, `calidad`, `tareas_obra`, `actas_reunion`, `ordenes_cambio`, `ordenes_compra`, `proveedores_gestion`. **Tercer lote agrupado, paso 1 completo (2026-08-03):** los últimos 23 tablas "solo de código" restantes del inventario (ver arriba) quedaron declaradas en 6 verticales por dominio — `planificacion_produccion` (`fases_obra`, `diario_obra`, `plan_semanal`, `rendimientos`, `field_reports`), `finanzas_obra` (`presupuesto_obra`, `presupuesto_lineas`, `costes_obra`, `cobros_cliente`, `gastos_dietas`, `licitaciones`), `seguridad_cumplimiento` (`registro_ambiental`, `seguros_obra`, `cae_documentacion`, `ausencias`, `libro_subcontratacion`, `toolbox_talks`), `relaciones_obra` (`correspondencia`, `contactos_obra`, `lecciones_aprendidas`, `cierre_obra_items`), `flota` (`flota_vehiculos`) y `nexus_experts` (aparte, dominio distinto sin tenant). Verificado (solo lectura, `sqlite_master`) que ninguna de las 23 existe todavía en D1 — a diferencia de los ocho verticales anteriores, el paso 2 de este lote no será un no-op, creará las tablas por primera vez. Con esto, **las 23 tablas "solo de código" del inventario original quedan todas declaradas** (ver `TASKS.md`, `ARC-011-FASE3-LOTE3`); paso 2 en adelante exige autorización explícita del Director. |
| ARC-012 | Resuelto | Tres columnas ausentes en producción por DDL silenciado | Alto | **Arreglado el 2026-08-02.** `planos.circuitos_json`, `inventario_seg.ubicacion` (cierra SEG-01 de verdad) y `empresas.retencion_config` (restaura la retención RGPD) aplicadas por el workflow manual y verificadas contra el esquema real. Runs 30722027660, 30722072138 y 30722103191. Causa raíz cerrada en ARC-013. **DDL en runtime de las tres columnas retirado el mismo día** (PR #54, mismo patrón que ADR-0011 paso 3): `ALTER TABLE` comentado con referencia a su migración; desplegado y verificado (`/health` healthy, columnas presentes tras el despliegue). |
| ARC-013 | Cerrado — corregido, desplegado y con persistencia | `catch` vacíos ocultan fallos de DDL | Alto | Causa raíz de ARC-012. **Corregido el 2026-08-02** (`eb772ee`): se introduce `runDDL()`, que nunca lanza pero registra por `console.error` todo error que no sea el duplicado esperado. 41 llamadas en `worker.js` y 7 en `alejandra-agente/worker.js` (regla de los dos cerebros). El alcance real era mayor que el estimado aquí: los 18 contados eran solo `ALTER`, pero los `CREATE TABLE` silenciados son la misma clase de riesgo. Se añade `ddlPaso()` para `runMigrations()`, donde el defecto era peor —cada paso etiquetaba cualquier error como «ya existe», dándole al operador un visto bueno falso— y ahora distingue aplicada / ya existía / ERROR (10 pasos). **Desplegado el 2026-08-02** (`alejandra-app-api` versión `9cfb30c3-...`, `alejandra-agente` versión `74234d68-...`, PR #49, ver `PROJECT_STATE.md`). El registro ya no se queda solo en `console.error`: ADR-0014 (aceptado y desplegado el mismo día) conectó `runDDL()`/`ddlPaso()` a `registrarTraza()`, así que todo error de DDL persiste en `alejandra_trazas` (`tipo='ddl_error'`), consultable vía `GET /admin/trazas` — la dependencia de ARC-008 quedó resuelta, no pendiente. **Verificación de segunda ronda (2026-08-03):** las 15 columnas/tabla restantes con DDL silenciado del inventario original (`reset_tokens`, `login_attempts`, `auth_nonces`, `partes_trabajo`, `fotos_obra`, `escaneos_remotos`, `tareas_obra`, `actas_reunion`, `control_calidad`, `punch_list`) se verificaron contra D1 real (lectura de metadatos, autorizada por el Director) y **las 15 están presentes** — a diferencia de ARC-012, esta ronda no encontró bugs activos nuevos. Detalle en `docs/architecture/07-INVENTARIO-DDL-RUNTIME.md`. |
| ARC-015 | **Cerrado — desplegado y verificado (2026-08-02)** | El esquema que se le describe a Alejandra no coincide con el real | Alto | El bloque `SCHEMA BASE DE DATOS` del prompt de `worker.js` (75 tablas) se escribió de memoria, no leyendo los `CREATE`. Alejandra genera SQL contra columnas que no existen. Corregidas las 8 tablas cuyo `CREATE` vive en `worker.js` y por tanto es autoritativo: `turnos`, `historial_mantenimientos`, `checklist_plantillas`, `checklist_registros`, `fotos_obra`, `docs_notas`, `login_attempts`, `vincular_tokens`. Se documentó además que `checklist_plantillas` y `checklists_plantillas` son **tablas distintas**, no una errata, y se añadieron al prompt las dos que faltaban. **Las ~29 tablas restantes verificadas contra D1 real** vía la auditoría remota de Cloudflare de F-0.2 (2026-08-02, autorizada por el Director): esquema verificado 57/60 correcto (`5c8b2b9`). **Desplegado el mismo día** (`alejandra-app-api` `9cfb30c3-...`, `alejandra-agente` `74234d68-...`, PR #49). Sin pendientes. |
| ARC-016 | **Cerrado — corregido, desplegado y verificado (2026-08-02)** | Chat anónimo del agente alcanzaba datos de cualquier empresa | **Crítico** | `/api/chat` y `/api/chat/stream` aceptan peticiones sin sesión por diseño, pero sin sesión el `empresa_id` salía del body **sin verificar**, así que el acotamiento por empresa lo elegía quien llamaba. 34 tools quedaban fuera de `TOOLS_REQUIEREN_SESION`, entre ellas las de datos de obra, inventario, documentos y proveedores, las `gestionar_*` que escriben, y las de GitHub que exponen código privado. Además `normalizarUsuarioId` resolvía un **nombre** a la cuenta real, de modo que un anónimo con `usuario_id:"Adrian"` obtenía su id y con él se le inyectaban al prompt sus 10 últimos mensajes privados —y podía escribir en su historial—, saltándose el control que sí tiene `/api/chat/history`. Corregido el 2026-08-02: gateo de las 34 tools, identidad anónima con prefijo `anon:` que no puede colisionar con un id real, y `empresa_id` del body ignorado sin sesión. 5 pruebas negativas nuevas (90/90). El chat anónimo sigue funcionando para consultas técnicas. **Desplegado el mismo día** (`alejandra-agente` `74234d68-...`, PR #49); verificado en producción. |
| ARC-017 | **Cerrado — corregido, desplegado y verificado (2026-08-02)** | El cron del agente ejecuta el modelo con privilegios de desarrollador | **Crítico** | **Corregido el 2026-08-02.** El flag `esDevVerificado` NO se puede bajar sin más: `puedeNotificarUsuario` depende de él y el cron sí necesita avisar a cualquier usuario. La barrera correcta es la lista de tools — lo que no se le ofrece al modelo, no lo puede invocar. Se añade `TOOLS_PROHIBIDAS_CRON` (9 tools) y `esInvocacionCron()`, que detecta al cron por identidad (`system`/`cron`), no por un flag que haya que acordarse de pasar. El cron pasa de 69 a 60 tools: pierde `ejecutar_deploy`, `rollback`, `patch_codigo`, `github_escribir`, `nexus_manage`, `test_endpoint`, `escribir_bd`, `configurar_alerta` y `tomar_decision`; conserva lectura, análisis y avisos. Sus escrituras deterministas (`tareas_alejandra`, `alejandra_ram`) van por SQL directo y no se ven afectadas. El usuario normal con sesión mantiene sus 62 tools, sin regresión. 4 pruebas negativas nuevas (94/94). Queda N0–N1 en términos de ADR-0006, que era la pregunta 3. **Desplegado el mismo día** (`alejandra-agente` `74234d68-...`, PR #49); verificado en producción. |
| ARC-014 | Riesgo aceptado temporalmente (decisión del Director, 2026-08-02; **revisado sin cambios el 2026-08-03**) | La aprobación de entorno no frena a un agente con token de administración | Medio | Detectado al aplicar ARC-012: el entorno `production` exige revisor, pero un agente que use un token con permisos de administración puede aprobar su propio despliegue vía API. La barrera protege frente a automatismos accidentales, no frente a un actor con ese token. **Decisión del Director (2026-08-02):** mientras el proyecto permanezca en fase de desarrollo con un único mantenedor, se acepta el riesgo sin mitigación adicional — no hay revisor distinto posible con un solo mantenedor. **Condición de reapertura:** en cuanto Alejandra entre en producción real o exista más de un mantenedor, será obligatorio exigir aprobación por una identidad distinta al solicitante para cualquier despliegue de producción (revisor distinto o token de agente sin permiso de aprobar entornos). **Revisado el 2026-08-03:** el Director confirmó explícitamente que ninguna de las dos condiciones de reapertura cambió; sigue como riesgo aceptado, sin acción de ingeniería pendiente. |
| ARC-019 | **Resuelto — ADR-0015 aceptado e implementado (2026-08-04)** | `sql_query` (`worker.js` raíz) permitía DDL sin la distinción N3 que ADR-0006 le da explícitamente a `run_migration`, y ninguna de las dos tools exigía confirmación humana para `CREATE TABLE`/`CREATE INDEX` | Cerrado | Detectado el 2026-08-02 durante `F-1.3-MIGRAR-RESTO-TOOLS`; ampliado el 2026-08-04 al redactar `docs/decisions/ADR-0015-CLASIFICACION-SQL-QUERY-Y-BARRERA-DDL.md`: `detectarSqlDestructivo()` solo disparaba ante `DROP`/`TRUNCATE`/`ALTER TABLE`/`DELETE`/`UPDATE`, dejando sin barrera el caso más común de migración (crear una tabla). **Decisión del Director (2026-08-04):** `sql_query` sube a N3; se extiende la barrera humana a `CREATE TABLE`/`CREATE INDEX` en las dos tools, con frase distinta (`CONFIRMO MIGRACION <código>`) para no mezclarla con `CONFIRMO BORRADO`; alcance ampliado a `alejandra-agente`. **Implementado:** nueva `detectarSqlCreacion()`, `FRASE_CONFIRMACION_MIGRACION`, `extraerCodigosConfirmacion()`/`exigirConfirmacionHumana()` generalizadas por frase, `ctx.codigosConfirmadosMigracion` en los dos puntos de entrada del bucle de tool-use (chat dev/Telegram). **`alejandra-agente` revisado, sin brecha equivalente:** `escribir_bd` ya rechazaba `CREATE` de raíz (lista blanca `INSERT`/`UPDATE`/`DELETE`/`REPLACE`, `DROP`/`ALTER`/`TRUNCATE` explícitamente prohibidos); `consultar_bd`/`configurar_alerta`/`exportar_datos` (rama `custom`) solo permiten `SELECT`. Sin cambios de código en `alejandra-agente`. `node --check worker.js` limpio; verificado a mano que `CREATE TABLE`/`CREATE INDEX`/`CREATE UNIQUE INDEX` activan la barrera nueva, `ALTER TABLE`/`DROP` siguen con la barrera existente, `SELECT`/`INSERT` sin cambios. Sin PR/desplegar todavía. |
| ARC-020 | En curso — rebanadas 1-7 completadas; catálogo N0+N1 completo bajo el Motor, N2/N3 solo trazadas (2026-08-07) | Contexto legacy global e integración del Motor de Decisión en Alejandra Chat | Crítico | Auditoría 2026-08-06: `buildAnthropicSystemBlocks()` leía `alejandra_ram`, `alejandra_errores`, `alejandra_memoria`, logs e historial sin filtro de empresa/usuario y los convertía en instrucciones del sistema. El fix fail-closed desactiva esas lecturas, sin tocar datos. **ADR-0020 aceptado por el Director el 2026-08-06:** la rebanada 1 conecta el Motor de Decisión a invocaciones N0 ofrecidas, registra traza previa y rechaza nombres no ofrecidos; N1-N3 mantienen sus gates actuales. **Rebanada 2 (2026-08-07):** análisis de las 47 trazas N0 (todas de cron/`consultar_bd`) confirmó el mecanismo; se amplía el piloto a todo el catálogo N0 (36 tools) y se completó el metadato ADR-0010 de 4 tools sin `nivel_riesgo` — ADR-0020 enmienda 1. **Rebanada 3 (2026-08-07, enmienda 2):** `registrarExplicabilidad()` gana implementación real (sin I/O: valida motivos/evidencia con contenido, no solo presencia); `decidirInvocacionN1Lectura()` nueva en `motor-decision.js`, exige sesión + explicabilidad (ADR-0009). Auditadas las 26 tools N1 del catálogo: solo `verificar_deploy` es de solo lectura confirmada (consulta GitHub Actions, sin escritura en D1/R2); el resto mezcla lectura/escritura por `accion` (`gestionar_*`) o escribe sin más. `TOOLS_N1_LECTURA_PILOTO` (`lib.js`) queda con ese único elemento a propósito. **Rebanada 4 (2026-08-07, enmienda 3):** cierra los dos puntos que quedaban del ADR original — contexto seguro se declara cumplido (ya satisfecho por `SEC-CHAT-CONTEXTO-LEGACY` + `memoria_consultar`, sin código nuevo) y política determinista se implementa reutilizando `validarDeclaracionTool()` (ADR-0010): una tool candidata al piloto (N0 o N1 lectura) con metadato ausente/inválido se rechaza en vez de asumirse disponible, sin ampliar qué tools gobierna el Motor. **Rebanada 5 (2026-08-07, enmienda 4):** clasificación N1 por invocación implementada. Auditados los `case` de las 6 tools CRUD compuestas (`gestionar_tarea/rfi/oc/acta/calidad`, `historico_materiales`): `listar`/`resumen`/`consultar`/`comparar` son puro `SELECT`; el resto escribe. `esInvocacionN1DeLectura()` (`lib.js`) decide por tool+`accion`, fail-closed. `TOOLS_N1_LECTURA_PILOTO` sigue con un solo elemento (`verificar_deploy`, tool entera); las 6 CRUD se gobiernan por `ACCIONES_N1_LECTURA_POR_TOOL`. **7 tools con al menos una invocación de lectura piloteada.** **Rebanada 6 (2026-08-07, enmienda 5):** refuerzo N2/N3, sin ampliar permisos — `decidirInvocacionN2N3()` siempre decide `'posponer'` (nunca `'invocar_tool'`) para una tool N2/N3, dejando traza explícita donde antes no había ninguna; `CONFIRMO BORRADO`/`CONFIRMO MIGRACION` no se tocan, siguen siendo la única barrera real. **Rebanada 7 (2026-08-07, enmienda 6):** `decidirInvocacionN1Lectura()` generalizada a `decidirInvocacionN1()` — ADR-0009 exige el mismo nivel `explicabilidad` para N1 sin distinguir lectura/escritura; la restricción anterior era cautela de pilotaje, no un límite de ADR-0006 (N1 = "reversible, acotado" por definición). `esInvocacionN1DeLectura()` deja de gatear, pasa a enriquecer la traza (`es_lectura`). El Motor gobierna ahora el catálogo N1 completo (26 tools). **Pendiente, sin decisión tomada:** implementar revisión humana asíncrona real para N2 (requiere ADR propio, cablear el canal de Telegram — cambio mayor, no autónomo); extensión a N3 sigue descartada por mandato de ADR-0006. Tests: cognitive-core 57/57 (con policy), agente 178/178. |
| ARC-021 | **Riesgo aceptado por decisión del Director (2026-08-07)** | `wrangler deploy` directo contra `alejandra-agente`, sin pasar por el workflow gobernado | Medio | Auditoría 2026-08-07: `wrangler deployments list --name alejandra-agente` muestra tres despliegues el mismo día — `96d21329` (10:45:18 UTC, corresponde al run `31170999186` de `deploy-alejandra-agente.yml`) y dos más sin ningún run de GitHub Actions asociado: `a92ec4ce` (12:55:58 UTC, commits `d6eb154`/`02ea344`) y `e8fba7ca` (16:25:01 UTC, 24s después del commit `b03e369`). Consistente con `wrangler deploy` directo desde sesión/terminal, sin workflow ni confirmación — la vía que `CLAUDE.md`/ADR-0001/ADR-0007 reservan a `deploy-alejandra-agente.yml` con aprobación del entorno `production`. El bypass lo autorizó el propio Director, por comodidad propia — no fue una acción no autorizada de ningún agente. **Decisión del Director (2026-08-07): se acepta `wrangler deploy` directo como práctica habitual**, mismo criterio que ARC-014 — mientras el proyecto tenga un único mantenedor en fase de desarrollo, no se exige pasar por `deploy-alejandra-agente.yml` en cada despliegue. `OPEN_TASK_SUMMARY.md` mantiene su recomendación del comando sin cambios. **Condición de reapertura (igual que ARC-014):** en cuanto exista producción real con impacto en terceros o más de un mantenedor, se retoma la exigencia de pasar siempre por el workflow con aprobación de entorno. Sin acción de ingeniería pendiente. |
| ARC-018 | Cerrado | Worker y bucket R2 huérfanos, sin documentar, con seguridad desactualizada | **Alto** | Auditoría remota de Cloudflare (F-0.2, 2026-08-02): la cuenta tenía un **tercer Worker**, `alejandra-worker`, y un **segundo bucket R2**, `alejandra-files` (el documentado es `alejandra-app-files`), ninguno mencionado en `CLAUDE.md`. `alejandra-worker` era un **fork abandonado de `worker.js`** (snapshot de principios de mayo, sin `ANTHROPIC_API_KEY`/tools), con CORS abierto a cualquier origen y sin ninguna de las mejoras SEC-01 a SEC-15, pero con rutas de escritura completas y alcanzables (login, INSERT/UPDATE/DELETE sobre las tablas de negocio). **Confirmado el 2026-08-02 vía dashboard de Cloudflare**: su binding `DB` apuntaba a la `alejandra-db` real de producción (mismo UUID `0c9eccde-78f1-476d-ac68-bf452bec0c62`) — era un segundo punto de escritura sin parchear sobre datos reales, no una base separada. **Resuelto con autorización explícita del Director:** `alejandra-worker` borrado del dashboard. Antes de tocar `alejandra-files`, se auditó su contenido completo (16 objetos, todos bajo `e1/dept/electrico/1/8/`): 4 eran duplicados exactos de fotos ya presentes en `alejandra-app-files`, pero **12 eran únicas** — fotos reales de una incidencia subidas el 23/04 a través del Worker huérfano, que nunca llegaron al bucket ni al sistema real. Migradas con `wrangler r2 object get/put` a `alejandra-app-files`, en la misma ruta, verificadas por tamaño de archivo idéntico byte a byte. Con el bucket huérfano ya sin contenido único, se vació (`wrangler r2 object delete`, 16/16, verificado con un `get` fallido tras el borrado) y se eliminó desde el dashboard. Ambos recursos huérfanos ya no existen en la cuenta. |
| ARC-022 | **Implementado y desplegado (2026-08-11), incluida migración D1 autorizada — control de accesos completo (usuarios + personal externo) con quiosco de autofichaje** | Foto de perfil + tarjetas con QR + quiosco de control de accesos | Medio | Pedido por el Director (2026-08-10/11), evolucionó de "tarjeta con QR para fichar" a un control de accesos tipo quiosco tras aclarar el caso de uso real: "se trata de dejarlo habilitado y que la gente pase su QR por ahí cuando entre, no que tú le des y luego pases la tarjeta". Decisiones tomadas antes de implementar (vía AskUserQuestion): el QR codifica el `codigo` de fichar/entrar ya existente; tarjeta imprimible (no solo QR digital); escaneo con cámara del móvil **y** con lector físico USB/Bluetooth (ambos emulan teclado, mismo campo de texto sirve para los dos, sin código distinto); cubre también personal externo (subcontratas sin cuenta), no solo `usuarios`; la ficha que aparece al fichar muestra foto+nombre+rol/departamento+empresa+DNI (si es externo) y un aviso SOLO si el reconocimiento médico o un carnet están REALMENTE caducados (no listado completo). Implementado: (1) foto de perfil para `usuarios` en `index.html`/`panel.html` (backend ya listo); (2) tarjeta imprimible con QR (`qrcodejs`, CDN) para `usuarios` (`index.html`/`panel.html`) y para `personal_externo` (`index.html`, sección Plantilla — `panel.html` no tiene pantalla de gestión de personal externo, se dejó fuera a propósito); (3) **migración D1 aplicada (autorizada explícitamente por el Director, `ALTER TABLE personal_externo ADD COLUMN codigo TEXT`, verificada contra el esquema real tras aplicarla)** — personal_externo no tenía código propio, se genera bajo demanda (`POST /personal-externo/:id/codigo`, idempotente) o al crearse (`crearPersonalExterno`); (4) `POST /fichajes/scan` (`ficharPorCodigo`, `worker.js`) generalizado para resolver el código contra `usuarios` O `personal_externo` de la misma empresa, mismas reglas de horario/retraso/dedupe que `crearFichaje`, y devuelve una ficha completa (foto, rol/departamento, DNI si es externo, nombre de empresa, y `alertas[]` si hay reconocimiento médico o carnet caducado — consulta `reconocimientos_medicos`/`carnets` por `usuario_id`/`externo_id`); (5) **`kiosco.html` nuevo** — pantalla de autofichaje independiente y a pantalla completa, pensada para dejar abierta en un monitor de entrada con un lector USB/Bluetooth conectado: login una vez (sesión larga, mismo sistema de sesión existente), campo invisible siempre reenfocado (reenfoque agresivo cada 800ms + en cada click/tap, para que nunca haga falta tocar nada), ficha de acceso grande con foto/nombre/estado/alertas que se autolimpia a los 4s; (6) `index.html` también recibió un campo de entrada manual (mismo mecanismo lector-como-teclado) dentro del modal "Fichar por QR" ya existente, para poder usar un lector USB/Bluetooth desde el móvil/tablet además de la cámara. **Hallazgo lateral corregido de paso:** la URL de cdnjs de `jsQR` (`jsQR/1.4.0/jsQR.min.js`) devolvía 404 (librería retirada de cdnjs) — rompía en silencio el escaneo de QR de bobinas/EPIs/herramientas, posiblemente desde hace tiempo en producción; corregida a jsdelivr. **Pendiente, sin decidir:** verificación en vivo del flujo completo con login real en Chrome (esta sesión solo verificó `index.html` visualmente contra el DOM; `panel.html` y `kiosco.html` se verificaron por sintaxis/revisión manual, no en el navegador, por limitaciones de la herramienta de pruebas con el tamaño de `panel.html` y con la navegación a archivos nuevos fuera del proyecto). |

## Criterio de priorización

Primero se resuelven bloqueos de coherencia, riesgos críticos de seguridad/producción y límites de permisos/datos; después modularidad/optimización. Ninguna prioridad sustituye aprobación explícita.

## Revisión de Fase 2

No se ha añadido alcance funcional fuera del Motor de Decisión. Las dependencias detectadas durante el diseño ya estaban registradas en ARC-001, ARC-003, ARC-004 y ARC-006; no se crean decisiones nuevas hasta la revisión de ADR-0004.

## Referencia de planificación

`MASTER_ROADMAP.md` organiza el orden y las dependencias globales. Este backlog conserva riesgos, deuda y decisiones pendientes; no duplica fases ni tareas inmediatas.

## Actualización de F-0.1

ARC-005 queda mitigado en los workflows versionados: CI, despliegue, D1 y secretos están separados. Permanece pendiente la validación remota y la configuración manual de los entornos `production` y `github-pages`, protección de `main` y mínimo privilegio de secretos/tokens.

### Alcance real de F-0.1 respecto al esquema (ARC-011)

Debe quedar registrado sin ambigüedad qué resuelve y qué no resuelve esta fase:

| F-0.1 | Alcance |
|---|---|
| ✅ Controla | Las migraciones ejecutadas por GitHub Actions: manuales, una a una, con confirmación, entorno protegido y sin `\|\| echo` que oculte fallos. |
| ❌ No elimina | Los cambios de esquema que `worker.js` ejecuta por su cuenta en producción, fuera de todo workflow y de toda aprobación. |
| ❌ No resuelve | La divergencia entre el esquema versionado (`schema_completo.sql` + 24 `migrate_*.sql`) y el esquema creado dinámicamente desde código. |

Consecuencia: la promoción deliberada a producción está mitigada **para el código**, no para el
esquema. Un despliegue aprobado puede seguir alterando la estructura de D1 sin que nadie lo
apruebe, porque el DDL viaja dentro del propio Worker.

### Trabajo futuro obligatorio (no pertenece a F-0.1)

1. **Inventariar** todos los `CREATE TABLE`, `ALTER TABLE` y demás cambios de esquema ejecutados
   desde código, en ambos Workers, con ubicación, tabla afectada y si están silenciados.
2. **Contrastar** ese inventario con el esquema real de D1 mediante consulta autorizada de solo
   lectura, y con las migraciones versionadas, para obtener la divergencia efectiva.
3. **Convertirlos progresivamente** en migraciones versionadas, idempotentes y verificables, con
   un migrador único, orden explícito y registro de aplicación.
4. **Retirar el DDL en runtime** solo después de que su equivalente versionado esté aplicado y
   verificado, vertical por vertical, nunca en bloque.
5. ~~Requiere ADR propio antes de tocar código.~~ **ADR-0011 aceptado el 2026-08-02** como
   estrategia de evolución (ver ARC-011 arriba). La estrategia está decidida; cada aplicación
   real contra D1 sigue exigiendo autorización explícita del Director.

Este refactor **no se ejecuta dentro de F-0.1**. F-0.1 se limita a dejar el riesgo registrado,
acotado y con la migración 008 bloqueada para que no se dispare por accidente.
